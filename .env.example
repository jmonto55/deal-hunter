# Neon Postgres connection string — get a free DB at https://neon.tech
# Use the "pooled" connection string (ends in -pooler.<region>.aws.neon.tech)
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
